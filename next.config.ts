import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["ajeuwbhvhr.cloudimg.io", "colony-recorder.s3.amazonaws.com"],
  },
};

export default nextConfig;
